import { Tokens } from 'marked';

/**
 * renders the listItems to markdown
 * @returns the renderer
 */
export default function listItemRenderer(listItem : Tokens.ListItem) : string {
    return ``;
}
