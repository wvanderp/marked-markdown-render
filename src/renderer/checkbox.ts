import { Tokens } from 'marked';

/**
 * renders the checkbox to markdown
 * @returns the renderer
 */
export default function checkboxRenderer(checkbox : Tokens.Checkbox) : string {
    return ``;
}
